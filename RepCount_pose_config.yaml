dataset: {
  csv_label_path: 'all_action.csv',
}
PoseRAC: {
  seed: 42,
  dim: 104,
  heads: 8,
  enc_layer: 6,
  all_key_points: 104,
  learning_rate: 0.001,
  alpha: 0.01
}
Action_trigger: {
  enter_threshold: 0.717,
  exit_threshold: 0.30,
  momentum: 0.4
}
